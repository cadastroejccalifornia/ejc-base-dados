Base container for every panel. Set `title` + `action` for section headers ("Categorias" · "Ver todos").

```jsx
<Card title="Avaliações recentes" action={<Select value="Todas" variant="plain" />}>…</Card>
```
