KPI card for the dashboard header row (membros ativos, média das avaliações, etc.).

```jsx
<StatCard label="Membros ativos" value="128" delta="+6 esta semana" accent="var(--cat-blue)" icon={<Icon name="users"/>} />
```
