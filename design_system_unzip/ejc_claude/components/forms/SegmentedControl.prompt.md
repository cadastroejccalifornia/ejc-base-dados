Tab switcher. Pill for scoped toggles (Todos / Equipes / Palestras); underline for top-level page tabs.

```jsx
<SegmentedControl variant="pill" options={["Todos","Equipes","Palestras"]} value={tab} onChange={setTab} />
```
